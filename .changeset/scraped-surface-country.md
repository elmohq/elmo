---
"@workspace/lib": patch
"@workspace/config": patch
---

Scraped surfaces are no longer hardcoded to the United States. `DATAFORSEO_LOCATION_CODE`, `CLORO_COUNTRY` and `BRIGHTDATA_SERP_COUNTRY` set the market a deployment measures from; all three default to the values they were previously fixed at, so existing instances are unchanged.
