---
"@workspace/web": patch
"@workspace/lib": patch
---

Fixed the admin workflows page counting platforms a brand doesn't track as overdue; each prompt is now measured against the platforms it actually runs.
