---
"@workspace/web": patch
---

The sign-in and sign-up pages now explain what Elmo does, which engines it tracks, and who already uses it — with self-hosted instances also shown what the managed cloud offers.
