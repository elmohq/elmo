---
"@workspace/lib": patch
---

Prevent duplicate organization memberships from concurrent identity syncs (whitelabel Auth0), deduplicating any existing duplicates and enforcing one membership per member.
