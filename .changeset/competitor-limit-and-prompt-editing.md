---
"@workspace/web": patch
"@workspace/lib": patch
"@workspace/config": patch
---

Track up to 500 competitors per brand, and keep a brand's prompts editable when it already holds more than the prompt limit.
