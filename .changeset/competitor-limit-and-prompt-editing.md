---
"@workspace/web": patch
"@workspace/lib": patch
"@workspace/config": patch
---

Track up to 500 competitors per brand, keep a brand's prompts editable when it already holds more than the prompt limit, and report rather than offer platform picks where they are set by whoever runs the deployment.
