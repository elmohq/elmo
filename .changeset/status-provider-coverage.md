---
"@workspace/web": patch
"@workspace/www": patch
---

The `/status` page now covers Grok (Olostep and BrightData), Gemini on OpenRouter, and Google AI Overview on BrightData and DataForSEO, and DeepSeek renders with its own name and logo instead of a generic icon.
