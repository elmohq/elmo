---
"@workspace/web": patch
"@workspace/lib": patch
"@workspace/cloud": patch
---

Dashboard URLs now name the workspace and the brand (`/app/org/{workspace}/brand/{brand}`), both of which you can rename in settings, a workspace switcher sits in the sidebar, and team and billing moved to workspace settings.
