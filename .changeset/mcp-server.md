---
"@workspace/web": patch
---

Add an MCP server endpoint at `/api/mcp` (enabled by setting `MCP_API_KEY`) that exposes Model Context Protocol tools to inspect AI-visibility performance, tracked models, prompts, and opportunities, and to create, update, and delete prompts.
