---
"@workspace/web": patch
"@workspace/lib": patch
---

Elmo instances now serve an MCP server at `/api/mcp`, so Claude Code, OpenCode, and other AI clients can read your visibility data and manage prompts directly. Connect with an API key from Settings → API keys.
