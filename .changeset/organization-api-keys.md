---
"@workspace/web": patch
"@workspace/lib": patch
"@workspace/api-spec": patch
---

Issue scoped API keys from a workspace and reach the whole product over `/api/v1`: brand analytics, individual model answers, tags, and the opportunities report, alongside the brands, prompts, and competitors the API already covered.
