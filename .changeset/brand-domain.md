---
"@workspace/web": patch
"@workspace/lib": patch
"@workspace/docs": patch
---

Brands now store a domain instead of a website URL, and the prompt wizard has a separate "Page to analyze" field for sub-brand pages.
