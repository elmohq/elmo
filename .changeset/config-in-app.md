---
"@workspace/web": patch
"@workspace/lib": patch
"@workspace/worker": patch
"@elmohq/cli": patch
---

Tracked AI platforms, run cadence, and provider credentials are now configured in the app, and `SCRAPE_TARGETS` is imported into the database on first boot.
