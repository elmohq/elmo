---
"@workspace/www": patch
---

The API reference pages now render their prose properly: inline code, headings, tables, and paragraph spacing in endpoint and schema descriptions no longer come through as unstyled text, and each endpoint's summary appears once, above the request builder.
