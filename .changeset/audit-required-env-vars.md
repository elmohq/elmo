---
"@workspace/config": patch
"@workspace/web": patch
"@workspace/lib": patch
---

Elmo's startup check now asks for a direct LLM key when onboarding needs one, counts provider keys saved in the app, and no longer requires the whitelabel parent-app link.
