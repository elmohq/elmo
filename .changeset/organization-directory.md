---
"@workspace/web": patch
"@workspace/config": patch
"@workspace/lib": patch
"@workspace/cloud": patch
---

The account menu lists every organization you belong to and the brands under each — sorted alphabetically — and cloud customers can create another organization.
