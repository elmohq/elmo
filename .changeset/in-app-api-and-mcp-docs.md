---
"@workspace/web": patch
"@workspace/api-spec": patch
---

Browse the API reference and MCP connection details inside the app: new API and MCP pages beside API Keys, both describing the deployment you are signed in to rather than linking out to hosted docs.
