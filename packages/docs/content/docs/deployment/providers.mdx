---
title: AI Providers
description: Configure which AI engines to track and which providers to use for scraping.
---

## Quick Start

The fastest way to get started is with [Olostep](https://olostep.com), which can scrape ChatGPT, Google AI Mode, and Copilot — the three most valuable engines for visibility tracking.

Set a single environment variable:

```
OLOSTEP_API_KEY=your-key-here
SCRAPE_TARGETS=chatgpt:olostep:online,google-ai-mode:olostep:online,copilot:olostep:online
```

This tells Elmo to track three engines, all using Olostep as the scraping provider, all with web search enabled.

import { Callout } from "fumadocs-ui/components/callout";

<Callout type="info">
It is almost always more valuable to track more prompts across the top 3 engines than to add more engines. Focus on prompt coverage first, engine breadth second.
</Callout>

## SCRAPE_TARGETS Format

The `SCRAPE_TARGETS` environment variable is a comma-separated list of scrape targets. Each target follows the format:

```
engine:provider[:model][:online]
```

| Position | Required | Description |
|----------|----------|-------------|
| 1 — `engine` | Yes | AI engine to track (`chatgpt`, `google-ai-mode`, `copilot`, etc.) |
| 2 — `provider` | Yes | How to reach it (`olostep`, `brightdata`, `direct`, `openrouter`, `dataforseo`) |
| 3 — `model` | For `direct` and `openrouter` | Model slug (e.g. `gpt-5-mini`, `anthropic/claude-sonnet-4`). May contain colons for OpenRouter variants. |
| 4 — `:online` | No | Append to enable web search. Omit for no web search. |

### Examples

```bash
# Scraping providers (no model needed)
chatgpt:olostep:online
google-ai-mode:brightdata:online

# Direct API (model required)
chatgpt:direct:gpt-5-mini:online
claude:direct:claude-sonnet-4

# OpenRouter (model required, may include variant suffix)
chatgpt:openrouter:openai/gpt-5-mini:online
perplexity:openrouter:perplexity/sonar-pro:online
```

### Backward Compatibility

If `SCRAPE_TARGETS` is **not set**, Elmo derives equivalent configuration from legacy API key environment variables:

| Legacy Variable | Equivalent Target |
|-----------------|-------------------|
| `OPENAI_API_KEY` | `chatgpt:direct:gpt-5-mini:online` |
| `ANTHROPIC_API_KEY` | `claude:direct:claude-sonnet-4` |
| `DATAFORSEO_LOGIN` | `google-ai-mode:dataforseo:online` |

Existing deployments continue working without changes.

## Pricing Comparison

Costs per query vary significantly by provider and engine. Use these tables to estimate your monthly spend.

### Scraping Providers (with Web Search)

Scraping providers load the actual AI engine web UI, so results always reflect the real consumer experience including web search.

| Engine | Olostep | BrightData | DataForSEO |
|--------|---------|------------|------------|
| ChatGPT | ~$0.05 (5 credits) | ~$0.03 | — |
| Google AI Mode | ~$0.03 (3 credits) | ~$0.03 | ~$0.04 |
| Google AI Overview | ~$0.03 (3 credits) | ~$0.03 | — |
| Copilot | ~$0.03 (3 credits) | ~$0.03 | — |
| Perplexity | ~$0.03 (3 credits) | ~$0.03 | — |
| Gemini | ~$0.03 (3 credits) | ~$0.03 | — |
| Grok | ~$0.03 (3 credits) | ~$0.03 | — |

### API Providers with Web Search

API providers call the model's API directly. Web search adds cost because the model performs live searches.

| Engine | Direct API | OpenRouter |
|--------|-----------|------------|
| ChatGPT | ~$0.01–0.03 | ~$0.01–0.04 |
| Claude | ~$0.05–0.15 | ~$0.05–0.15 |
| Perplexity | — | ~$0.005 |
| Gemini | — | ~$0.01 |
| Grok | — | ~$0.02 |

### API Providers without Web Search

Without web search, API calls are cheaper but results may not reflect what users see in the actual product.

| Engine | Direct API | OpenRouter |
|--------|-----------|------------|
| ChatGPT | ~$0.005 | ~$0.005 |
| Claude | ~$0.01 | ~$0.01 |
| Perplexity | — | ~$0.003 |
| Gemini | — | ~$0.005 |
| Grok | — | ~$0.01 |

<Callout type="warn">
Pricing is approximate and based on typical query lengths (~500 tokens in, ~1000 tokens out). Actual costs depend on response length, model version, and provider pricing changes. Check each provider's pricing page for current rates.
</Callout>

## What to Track

### Essential (Top 3)

These engines cover the vast majority of AI-assisted search traffic and should be tracked by every deployment:

- **ChatGPT** with web search — the largest AI assistant by usage. Use `:online` to match the real user experience.
- **Google AI Mode** — Google's AI-generated answers are shown to billions of search users. Always uses web search.
- **Copilot** — Microsoft's AI assistant, integrated into Bing and Edge. Use `:online`.

### Sometimes Worth Tracking

- **Perplexity** — a popular AI search engine. Lower volume than the top 3, but growing. Inexpensive via OpenRouter.

### Usually Not Worth the Cost

- **Claude** — expensive, especially with web search enabled (~$0.15/query). Most users interact with Claude for writing/coding, not web search.
- **Gemini** — largely redundant with Google AI Mode for visibility tracking purposes.
- **Grok** — low market share. Track only if your audience skews toward X/Twitter users.

## Provider Details

### Olostep

**Best for**: Most deployments. Broad engine support, simple setup.

- Supports: ChatGPT, Google AI Mode, Google AI Overview, Gemini, Copilot, Perplexity, Grok
- Requires: `OLOSTEP_API_KEY`
- Scrapes the actual web UI, so results match the real user experience
- Credit-based pricing (3–5 credits per query depending on engine)

### BrightData

**Best for**: High-volume deployments that need per-engine dataset control.

- Supports: ChatGPT, Google AI Mode, Google AI Overview, Gemini, Copilot, Perplexity, Grok
- Requires: `BRIGHTDATA_API_TOKEN` plus per-engine dataset IDs (e.g. `BRIGHTDATA_DATASET_CHATGPT`)
- Uses BrightData's Web Scraper API with engine-specific datasets
- More configuration overhead but competitive per-query pricing

### OpenRouter

**Best for**: Accessing multiple AI models through a single API key.

- Supports: ChatGPT, Claude, Perplexity, Gemini, Grok (and any model on OpenRouter)
- Requires: `OPENROUTER_API_KEY`
- Calls model APIs, not web UIs — results may differ from what end users see
- Web search available via the `:online` model variant
- Default models: `openai/gpt-5-mini`, `anthropic/claude-sonnet-4`, `perplexity/sonar-pro`, `google/gemini-2.5-flash`, `x-ai/grok-3-mini`

### Direct APIs

**Best for**: When you already have API keys and want the lowest latency.

- **Direct OpenAI** (`direct` or `direct-openai`): Requires `OPENAI_API_KEY`. Supports ChatGPT engine only.
- **Direct Anthropic** (`direct-anthropic`): Requires `ANTHROPIC_API_KEY`. Supports Claude engine only.
- Lowest latency since there's no intermediary
- Web search is supported on both (OpenAI via `web_search_preview` tool, Anthropic via `web_search` tool)

### DataForSEO

**Best for**: Google AI Mode tracking specifically, or if you already use DataForSEO for SEO data.

- Supports: Google AI Mode only
- Requires: `DATAFORSEO_LOGIN` and `DATAFORSEO_PASSWORD`
- Uses DataForSEO's SERP API for structured AI Mode results
- Does not support web search toggle (always includes search context)

## Web Search Configuration

The `:online` suffix in `SCRAPE_TARGETS` enables web search for that target. This is important because AI engines behave differently with and without web access:

- **With web search** (`:online`): The model searches the web before answering. Results include citations and reflect what users see when they ask questions in the real product. **Recommended for visibility tracking.**
- **Without web search**: The model answers from its training data only. Cheaper, but doesn't represent the real user experience for most engines.

### Recommendations Per Engine

| Engine | Web Search Recommended? | Notes |
|--------|------------------------|-------|
| ChatGPT | Yes | Most users have web search enabled by default |
| Google AI Mode | Always on | AI Mode inherently uses web search |
| Copilot | Yes | Bing-powered web search is core to Copilot |
| Perplexity | Yes | Perplexity is fundamentally a search engine |
| Claude | Optional | Claude is often used without web search; track with `:online` only if you want to measure web-search-mode visibility |
| Gemini | Yes | Most Gemini users interact via Google, which includes search |
| Grok | Optional | Depends on your audience |

For scraping providers (Olostep, BrightData), web search is inherent in the scrape — the `:online` flag is still accepted for consistency but the scrape always reflects the real UI which includes search.

## Brand-Level Overrides

By default, every brand in your workspace is evaluated against all configured `SCRAPE_TARGETS` engines. You can narrow this per brand using the `enabled_engines` column on the brands table.

When `enabled_engines` is set for a brand (e.g. `["chatgpt", "google-ai-mode"]`), only those engines will be used for that brand's prompts. When `NULL` (the default), all configured engines apply.

This is useful when:
- Different brands operate in different markets where certain engines matter more
- You want to reduce costs by only tracking the most relevant engines per brand
- Some brands are experimental and only need limited tracking
