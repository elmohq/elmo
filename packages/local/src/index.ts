/**
 * @workspace/local - Local development deployment package
 *
 * Provides local-mode implementation:
 * - createLocalDeployment() factory (no-auth fixed user session)
 * - OptimizeButton stub (returns null)
 */

export { createLocalDeployment } from "./auth-provider";
