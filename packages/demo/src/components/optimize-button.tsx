/**
 * OptimizeButton stub for demo deployments
 * Re-exports from local since behavior is the same (returns null)
 */
export { OptimizeButton, type OptimizeButtonProps } from "@workspace/local/components/optimize-button";
