{
  "name": "@elmohq/monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "lint": "turbo lint",
    "format": "turbo format",
    "test": "vitest run",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}