{
  "name": "e2e",
  "version": "0.2.19",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "check-types": "tsc --noEmit",
    "test:e2e": "playwright test --project=local",
    "test:e2e:cloud": "playwright test --project=cloud",
    "test:e2e:whitelabel": "playwright test --project=whitelabel",
    "test:e2e:demo": "playwright test --project=demo",
    "test:api": "mkdir -p test-results && cd bruno && bru run -r --env local --exclude-tags cloud,demo --reporter-junit ../test-results/api-junit.xml --reporter-json ../test-results/api-report.json",
    "test:api:cloud": "mkdir -p test-results && cd bruno && bru run -r --env cloud --tags cloud --reporter-junit ../test-results/api-cloud-junit.xml --reporter-json ../test-results/api-cloud-report.json",
    "test:api:demo": "mkdir -p test-results && cd bruno && bru run -r --env demo --tags demo --reporter-junit ../test-results/api-demo-junit.xml --reporter-json ../test-results/api-demo-report.json",
    "seed": "tsx seed.ts"
  },
  "dependencies": {
    "@workspace/lib": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "@types/pg": "^8.21.0",
    "@usebruno/cli": "4.0.0",
    "pg": "^8.23.0",
    "pg-boss": "^12.27.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
