meta {
  name: re-enabling past the limit is rejected
  type: http
  seq: 8
  tags: [
    cloud
  ]
}

patch {
  url: {{baseUrl}}/api/v1/prompts/{{operatorPromptId}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{cappedKey}}
}

body:json {
  {
    "enabled": true
  }
}

assert {
  res.status: eq 409
  res.body.code: eq prompt_limit
}
