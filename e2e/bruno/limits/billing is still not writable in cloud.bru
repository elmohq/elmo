meta {
  name: billing is not writable even where billing is real
  type: http
  seq: 17
  tags: [
    cloud
  ]
}

patch {
  url: {{baseUrl}}/api/v1/organizations/{{cappedOrgId}}/billing
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{cappedKey}}
}

body:json {
  {
    "plan": "business"
  }
}

assert {
  res.status: eq 405
  res.body.code: eq method_not_allowed
}
