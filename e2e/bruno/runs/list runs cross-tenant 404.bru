meta {
  name: another tenant's runs are not found
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/prompts/{{nikePromptId}}/runs?startDate={{startDate}}&endDate={{endDate}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{orgFullKey}}
}

params:query {
  startDate: {{startDate}}
  endDate: {{endDate}}
}

assert {
  res.status: eq 404
  res.body.error: eq Not Found
}
