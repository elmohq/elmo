meta {
  name: the read scope alone is enough
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/brands/{{brandId}}/opportunities
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{orgReadOnlyKey}}
}

assert {
  res.status: eq 200
}
