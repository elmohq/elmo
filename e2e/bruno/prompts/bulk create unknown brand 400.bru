meta {
  name: a bulk create naming an unknown brand
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/api/v1/prompts/bulk
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{orgFullKey}}
}

body:json {
  {
    "brandId": "does-not-exist",
    "prompts": [{ "value": "orphan" }]
  }
}

assert {
  res.status: eq 400
  res.body.error: eq Validation Error
  res.body.message: contains not found
}
