meta {
  name: a prompt reports its grounded platforms
  type: http
  seq: 27
}

get {
  url: {{baseUrl}}/api/v1/prompts/{{promptId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{orgFullKey}}
}

assert {
  res.status: eq 200
  res.body.premiumModels: isArray
}
