meta {
  name: a window is required
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/brands/{{brandId}}/analytics
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{orgFullKey}}
}

assert {
  res.status: eq 400
  res.body.code: eq validation_error
}
